# Carregado automaticamente pelo Next.js quando NODE_ENV=development (next dev).
# Não é segredo (é só a URL pública da API), por isso fica versionado — assim o
# ambiente de dev nunca depende de configurar nada manualmente.
NEXT_PUBLIC_BASE_API_URL=http://localhost:8000
